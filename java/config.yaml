provider:
  default:
    language: 11
  digitalocean:
    language: 11

sources:
  - src/**/*.java
  - pom.xml

binaries:
  - target/server-1.0.0.jar

files:
  - src/**/*.properties
  - src/**/*.xml
  - src/**/*.jsp

binaries:
  - target/server-1.0.0.jar

deps:
  - java-11-openjdk
